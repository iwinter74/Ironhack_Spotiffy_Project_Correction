<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/style.css">
</head>

<body>
    <section class="search-container">
        <% artistData.forEach(element => { %>
        <figure>

            <% element.images.forEach(elt => { %>
            <img src="<%= elt.url %>" alt="">
            <% }) %>

            <figcaption>
                <h2>
                    <a href="/albums/<%= element.id %>">
                        <%= element.name %>
                    </a>

                </h2>
            </figcaption>
        </figure>
        <% }) %>
    </section>
</body>

</html>